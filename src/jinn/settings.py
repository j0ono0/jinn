# Application settings and default values.


# Source folders.
# Template and content folders are concatenated with
# PROJECT_PATH to find source files where explicit paths are not supplied.
PROJECT_PATH = "."
TEMPLATE_SOURCE = "templates"
CONTENT_SOURCE = "content"

# Destination folders.
# Static and media folders are concatenated with
# BUILD_PATH to create an output files destination where explicit paths are not supplied.
BUILD_PATH = "build"
STATIC_DESTINATION = "assets"
MEDIA_DESTINATION = "media"
